---
import Layout from "../layouts/Layout.astro";
import VioletHero from "../components/VioletHero.astro";
import ThemeController from "../components/ThemeController.astro";
import { getCollection } from "astro:content";

const posts = await getCollection("blog", ({ data }) => !data.draft);
const latestPosts = posts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
---

<Layout title="Vigarden | Zyrexnn's Room">
  <ThemeController slot="header-right" />

  <!-- Hero Banner Area -->
  <section class="relative h-[60vh] min-h-[450px] w-full flex flex-col items-center justify-center text-center overflow-hidden bg-[#0D1117]">
    <div class="absolute inset-0 z-0">
      <img
        id="theme-gif"
        src="/images/violet-light.gif"
        alt=""
        class="w-full h-full object-cover opacity-80"
        loading="eager"
      />
      <div class="absolute inset-0 bg-gradient-to-tr from-black/50 via-transparent to-black/30"></div>
    </div>

    <div class="z-30 space-y-6 px-6" transition:animate="fade">
      <h1 class="text-6xl md:text-[8rem] font-heading text-white drop-shadow-[0_15px_15px_rgba(0,0,0,0.6)] tracking-tighter leading-none">
        わたしの部屋
      </h1>
      <p class="font-body text-white/90 tracking-[0.8em] text-xs sm:text-base uppercase opacity-80">
        — Auto Memories Doll Service —
      </p>
    </div>
    
    <div class="absolute bottom-12 left-12 z-40 transform scale-75 sm:scale-110 origin-bottom pointer-events-none drop-shadow-2xl">
       <VioletHero />
    </div>

    <!-- Moving Ocean -->
    <div class="absolute bottom-0 left-0 w-full overflow-hidden leading-[0] z-20">
      <svg class="moving-wave" xmlns="http://www.w3.org/2000/svg" viewBox="0 24 150 28" preserveAspectRatio="none">
        <defs>
          <path id="gentle-wave" d="M-160 44c30 0 58-18 88-18s 58 18 88 18 58-18 88-18 58 18 88 18 v44h-352z" />
        </defs>
        <g class="parallax">
          <use href="#gentle-wave" x="48" y="0" fill="currentColor" class="text-paper/20 dark:text-dark-bg/20" />
          <use href="#gentle-wave" x="48" y="3" fill="currentColor" class="text-paper/40 dark:text-dark-bg/40" />
          <use href="#gentle-wave" x="48" y="5" fill="currentColor" class="text-paper/60 dark:text-dark-bg/60" />
          <use href="#gentle-wave" x="48" y="7" fill="currentColor" class="text-paper dark:text-dark-bg" />
        </g>
      </svg>
    </div>
  </section>

  <!-- Main Content Layout -->
  <main class="max-w-[1700px] mx-auto px-6 sm:px-8 lg:px-12 py-16 relative z-30">
    <div class="flex flex-col lg:flex-row gap-8 items-start">
      
      <!-- LEFT SIDEBAR: PROFILE & NOTICE -->
      <aside class="w-full lg:w-[320px] flex-shrink-0 space-y-8 sticky top-28">
        
        <!-- Profile Card -->
        <section class="bg-[#1e1a1b] dark:bg-[#161213] rounded-[2rem] p-8 border border-white/5 shadow-2xl flex flex-col items-center">
          <div class="w-full aspect-square rounded-2xl overflow-hidden border-4 border-white/10 shadow-lg mb-6">
             <img src="/images/charviolet-removebg-preview.png" alt="Zyrexnn" class="w-full h-full object-cover" />
          </div>
          
          <div class="text-center space-y-4 w-full">
            <h2 class="text-2xl font-heading text-white tracking-wide">Zyrexnn</h2>
            <!-- Japanese bio placeholder from ref -->
            <p class="text-[11px] text-white/50 leading-relaxed">
               世界は大きい、君は行かなければならない
            </p>
            
            <div class="w-10 h-[2px] bg-accent/30 mx-auto"></div>

            <!-- Social Links Panel -->
            <div class="grid grid-cols-5 gap-2 w-full pt-4">
               <div class="h-10 bg-white/5 rounded-xl flex items-center justify-center opacity-40">📺</div>
               <div class="h-10 bg-white/5 rounded-xl flex items-center justify-center opacity-40">🎮</div>
               <a href="https://github.com/Zyrexnn" target="_blank" class="h-10 bg-white/10 hover:bg-white/20 rounded-xl flex items-center justify-center text-white transition-all">
                  <svg viewBox="0 0 24 24" fill="currentColor" class="w-5 h-5"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.041-1.416-4.041-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
               </a>
               <div class="h-10 bg-white/5 rounded-xl flex items-center justify-center opacity-40">📱</div>
               <div class="h-10 bg-white/5 rounded-xl flex items-center justify-center opacity-40">💬</div>
            </div>
          </div>
        </section>

        <!-- Notice Card -->
        <section class="bg-[#1e1a1b] dark:bg-[#161213] rounded-[2rem] p-6 border border-white/5 shadow-2xl relative overflow-hidden">
           <div class="flex items-center gap-2 mb-4">
              <span class="text-xs text-accent">📢</span>
              <h3 class="font-heading text-sm text-white/90">お知らせ</h3>
              <div class="absolute right-4 top-6 w-4 h-4 rounded bg-white/5 flex items-center justify-center text-[8px] text-white/40">×</div>
           </div>
           <p class="text-[11px] text-white/40 italic leading-relaxed">
              "ようこそ！これはサンのブログです。自動手記人形サービスも受付中です。"
           </p>
        </section>
      </aside>

      <!-- RIGHT SIDE: POSTS GRID (Mizuki Card Style) -->
      <section class="flex-1 grid grid-cols-1 xl:grid-cols-2 gap-6">
        {latestPosts.map(post => (
          <a href={`/blog/${post.id}/`} class="group block h-full">
            <article class="relative h-full bg-[#1e1a1b] dark:bg-[#161213] rounded-3xl border border-white/5 hover:border-accent/30 transition-all duration-500 overflow-hidden flex shadow-xl">
               <!-- Shaded Side Area for Arrow -->
               <div class="absolute right-0 top-0 bottom-0 w-16 bg-white/5 flex items-center justify-center group-hover:bg-white/10 transition-colors">
                  <div class="text-white/40 group-hover:text-white transition-colors">
                     <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" class="w-5 h-5"><path d="M9 18l6-6-6-6" /></svg>
                  </div>
               </div>

               <!-- Main Content Area -->
               <div class="flex-1 p-8 pr-24 space-y-4">
                  <!-- Header with Pushpin & Title -->
                  <div class="flex gap-3">
                     <span class="text-accent/60 text-lg">📌</span>
                     <h3 class="text-xl md:text-2xl font-heading text-white group-hover:text-accent transition-colors">
                        {post.data.title}
                     </h3>
                  </div>

                  <!-- Metadata Badges -->
                  <div class="flex flex-wrap gap-4 text-[10px] items-center text-white/40 uppercase tracking-widest">
                     <div class="flex items-center gap-1.5 bg-white/5 px-2.5 py-1 rounded-lg">
                        <span>📅</span> <span>{post.data.pubDate.toLocaleDateString()}</span>
                     </div>
                     <div class="flex items-center gap-1.5 bg-white/5 px-2.5 py-1 rounded-lg">
                        <span>🏷️</span> <span>{post.data.category}</span>
                     </div>
                     <div class="flex items-center gap-1.5 bg-white/5 px-2.5 py-1 rounded-lg">
                        <span>📜</span> <span>1426 文字</span>
                     </div>
                  </div>

                  <!-- Description -->
                  <p class="text-white/40 text-sm leading-relaxed line-clamp-2">
                     {post.data.description}
                  </p>

                  <!-- Tags -->
                  <div class="flex gap-2 pt-2">
                     {post.data.tags.map(tag => (
                        <span class="text-[9px] bg-white/10 text-white/70 px-3 py-1 rounded-full border border-white/10 group-hover:border-accent/30 transition-colors">
                           #{tag}
                        </span>
                      ))}
                  </div>
               </div>
               
               <!-- Left Decorative Accent (Vertical Bar) -->
               <div class="absolute left-0 top-8 bottom-8 w-1 bg-accent/20 group-hover:bg-accent rounded-r-full transition-all"></div>
            </article>
          </a>
        ))}
      </section>

    </div>
  </main>

  <!-- Waves Background (Consistent with Ref) -->
  <style>
    .moving-wave { width: 100%; height: 100px; }
    .parallax > use { animation: move-forever 25s cubic-bezier(.55,.5,.45,.5) infinite; }
    .parallax > use:nth-child(1) { animation-delay: -2s; animation-duration: 7s; }
    .parallax > use:nth-child(2) { animation-delay: -3s; animation-duration: 10s; }
    .parallax > use:nth-child(3) { animation-delay: -4s; animation-duration: 13s; }
    .parallax > use:nth-child(4) { animation-delay: -5s; animation-duration: 20s; }
    @keyframes move-forever { 0% { transform: translate3d(-90px,0,0); } 100% { transform: translate3d(85px,0,0); } }
  </style>
</Layout>
